# snorse.com

Static, accessible GitHub Pages site for the shared snorse-bot service, styled like a readable late-1990s hacker web shrine. It has no analytics, cookies, external fonts, or payment handling.

Set the Signal username, aggregate capacity endpoint, Ko-fi URL, and project link in `config.js`. When `kofiUrl` is empty, the external support button stays hidden while the costs and support policy remain readable.
